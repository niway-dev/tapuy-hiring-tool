{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "legacy:dev": "bun run --hot src/index.ts",
    "dev": "wrangler dev --port 3000 --inspector-port 9229",
    "start": "bun run dist/index.js",
    "deploy": "wrangler deploy",
    "cf:gen-types": "wrangler types --env-interface CloudflareBindings",
    "cf:login": "wrangler login",
    "cf:whoami": "wrangler whoami"
  },
  "dependencies": {
    "@better-auth/expo": "catalog:",
    "@elysiajs/cors": "catalog:",
    "@interviews-tool/application": "workspace:*",
    "@interviews-tool/domain": "workspace:*",
    "@interviews-tool/infra-auth": "workspace:*",
    "@interviews-tool/infra-db": "workspace:*",
    "@interviews-tool/infra-env": "workspace:*",
    "@interviews-tool/infra-prisma-db": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "elysia": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@interviews-tool/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "^5",
    "wrangler": "4.42.1"
  }
}
